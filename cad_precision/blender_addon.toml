schema_version = "1.0.0"

id = "cad_precision"
name = "CAD Precision Transform"
version = "0.1.0"
blender_version_min = "3.6.0"
license = ["SPDX:GPL-2.0-or-later"]
maintainer = "OpenAI"
tagline = "Deterministic numeric transforms with optional pivot overrides"

type = "add-on"

[permissions]
files = """No file system access required."""

[platforms]
windows = true
linux = true
macos = true

[website]
homepage = ""

[build]
paths = ["__init__.py"]
